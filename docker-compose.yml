name: outbound-url-guard-bundle
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: outbound-url-guard-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    command: ["tail", "-f", "/dev/null"]
    networks:
      - outbound-url-guard-bundle-network

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  outbound-url-guard-bundle-network:
    driver: bridge
